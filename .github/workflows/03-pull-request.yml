name: On Push Main Workflow

on: 
  pull_request:
    branches:
      - main
      
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5.0.0
      - name: Imprimir Message
        run: |
          echo "Desplegando desde main"
